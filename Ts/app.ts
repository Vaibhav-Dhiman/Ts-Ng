function add(n1:number, n2:number): number {
    return n1+n2;
}

function printResult(num:number) {
    console.log("result" + num);
}

function addHandler(n1: number, n2: number) {
    const res = n1+n2;
    
}

let combineValues : (a: number, b: number) => number;


printResult(add(23,45))